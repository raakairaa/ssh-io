name: SSHX Remote Access

on:
  workflow_dispatch:

jobs:
  sshx:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Install SSHX
        run: |
          curl -sSf https://sshx.io/get | sh

      - name: Start SSHX Session
        run: |
          ~/.local/bin/sshx
